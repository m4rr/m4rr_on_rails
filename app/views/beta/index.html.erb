<p id="notice"><%= notice %></p>

<h1>Listing Beta</h1>

<table>
  <thead>
    <tr>
      <th>First name</th>
      <th>Last name</th>
      <th>Email</th>
      <th>Desc</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @beta.each do |betum| %>
      <tr>
        <td><%= betum.first_name %></td>
        <td><%= betum.last_name %></td>
        <td><%= betum.email %></td>
        <td><%= betum.desc %></td>
        <td><%= link_to 'Show', betum %></td>
        <td><%= link_to 'Edit', edit_betum_path(betum) %></td>
        <td><%= link_to 'Destroy', betum, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Betum', new_betum_path %>
