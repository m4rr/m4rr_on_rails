language: minimal
dist: xenial

branches:
  only:
    - master

# addons:
#   ssh_known_hosts:
#     - $sshaddr

install:
  # - echo \"[$sshaddr]:$sshport ssh-rsa $dopubkey\" >> $HOME/.ssh/known_hosts
  - echo \"$dopubkey\" >> $HOME/.ssh/known_hosts

script:
  - ssh -o "StrictHostKeyChecking no" m4rr@$sshaddr -p $sshport "exit"

# \"$sshcomms\"
