language: minimal
dist: xenial

branches:
  only:
    - master

# addons:
#   ssh_known_hosts:
#     - $sshaddr

install:
  # - echo \"[$sshaddr]:$sshport ssh-rsa $dopubkey\" >> $HOME/.ssh/known_hosts
  - echo "$sshaddr ssh-rsa $dopubkey" >> $HOME/.ssh/known_hosts
  - cat $HOME/.ssh/known_hosts
  - ls $HOME/.ssh/
  - cat $HOME/.ssh/id_rsa.pub
  # - echo \"ssh-rsa $dopubkey\" >> $HOME/.ssh/authorized_keys

script:
  - ssh -v -o "StrictHostKeyChecking no" m4rr@$sshaddr -p $sshport "exit"

# \"$sshcomms\"
